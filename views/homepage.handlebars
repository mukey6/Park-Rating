<ol class="post-list">
  <li>
    <article class="post">
      <div class="title">
        <a href="{{post_url}}" target="_blank">{{title}}</a>
        <span>({{park_name}})</span>
      </div>
      <div class="meta">
 {{user.username}} Gave it a rate of a {{posts.park_rate}} on
        {{created_at}}
        
        {{!-- <a href="/post/{{id}}">{{comments.length}} comment(s)</a> --}}
      </div>
    </article>
  </li>
</ol>